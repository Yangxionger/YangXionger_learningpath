# Day1总结

学会：
1.Python输入输出
2.Python字典，列表，这两个的一些基本功能，读取等等
字典的get(key,default_value)
3.try-except异常处理
4.json读写
5.Git基本流程：git init-git add.--git commit -m " "--git branch -M main(将主仓库master改成main)
6.力扣刷题 
题1哈希map unordered_map去解决查找数组中两个相加等于target的下标 
题206链表 三指针原地反转链表
题128 最长连续序列 找到起始的数字 unordered_set去重